{
  "schema": "rustorr.r6.deferred-routes.v1",
  "entries": [
    {"scenario": "deferred-mcp", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true},
    {"scenario": "deferred-shutdown", "route": "POST /shutdown", "owner": "service-control", "reason": "Service and install control routes are outside R6; POST is absent from this reference build.", "follow_up_stage": "R9", "expected_difference": false},
    {"scenario": "r7-root", "route": "GET /", "owner": "web-ui", "reason": "The reference's bundled web UI is intentionally not carried over (user decision 2026-09-23); R8 ships a new interface.", "follow_up_stage": "R8", "expected_difference": true},
    {"scenario": "r7-stat", "route": "GET /stat", "owner": "web-api", "reason": "Accepted permanent difference (R7 closure): MatriX.145 prints anacrolix's internal client dump, which has no counterpart in librqbit; Rustorr prints the same per-torrent facts in its own format. No client parses this diagnostic page.", "follow_up_stage": "none", "expected_difference": true},
    {"scenario": "r7-dlna-icon-0", "route": "GET :9080/deviceIcon/0", "owner": "dlna", "reason": "The reference serves TorrServer's own artwork; Rustorr ships its own 48x48 PNG icon under the same path and type.", "follow_up_stage": "R8", "expected_difference": true},
    {"scenario": "r7-dlna-icon-1", "route": "GET :9080/deviceIcon/1", "owner": "dlna", "reason": "The reference serves TorrServer's own artwork; Rustorr ships its own 120x120 PNG icon under the same path and type.", "follow_up_stage": "R8", "expected_difference": true},
    {"scenario": "r7-dlna-icon-thumb", "route": "GET :9080/icon", "owner": "dlna", "reason": "Without ffmpegthumbnailer dms falls back to device icon 0, which is Rustorr's own artwork.", "follow_up_stage": "R8", "expected_difference": true},
    {"scenario": "r7-dlna-root-desc-default-name", "route": "GET :9080/rootDesc.xml", "owner": "dlna", "reason": "Accepted permanent difference of the test stand (R7 closure): the default DLNA name embeds the process user's full name; the reference container runs as root, Rustorr as the unprivileged rustorr user without one. The naming rules are unit-tested and identical for the same user.", "follow_up_stage": "none", "expected_difference": true},
    {"scenario": "r7-mcp-initialize", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true},
    {"scenario": "r7-mcp-tools-list", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true},
    {"scenario": "r7-mcp-server-info", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true},
    {"scenario": "r7-mcp-list-torrents", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true},
    {"scenario": "r7-mcp-no-session", "route": "POST /mcp", "owner": "mcp", "reason": "MCP is deferred past the first version (user decision 2026-09-23): an AI-agent client over the API Rustorr already serves; existing players do not use it.", "follow_up_stage": "post-R10", "expected_difference": true}
  ]
}
